module.exports = {
  dependency: {
    platforms: {
        android: {
            sourceDir: './lib/android',
        },
    },
  },

    assets: ["./src/Utils/fonts/"], // stays the same
  };